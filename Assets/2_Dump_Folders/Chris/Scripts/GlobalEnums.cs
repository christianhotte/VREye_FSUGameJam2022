using System.Collections;
using System.Collections.Generic;
using UnityEngine;

namespace GlobalEnums
{
    public enum HandType { Left, Right, Both, None }
    public enum GripType { Open, Fist, GrabLocked, GrabFree, Slap }
}
